import { ModifyTablePipe } from './modify-table.pipe';

describe('ModifyTablePipe', () => {
  it('create an instance', () => {
    const pipe = new ModifyTablePipe();
    expect(pipe).toBeTruthy();
  });
});
